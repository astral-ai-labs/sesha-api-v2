/* ==========================================================================*/
// index.ts — Database low-level API
/* ==========================================================================*/
// Purpose: Low-level database access for raw queries and schema
// Sections: Exports

// Database client for raw queries
export { db } from './client';

// Schema for table references and relations
export * from './schema'; 